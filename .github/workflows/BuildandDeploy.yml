name: Build and Deploy

on:
  push:
    branches:
      - main

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout main
      uses: actions/checkout@v1
      with:
        submodules: true

    - name: Hugo Deploy GitHub Pages
      uses: benmatselby/hugo-deploy-gh-pages@master
      env:
        HUGO_VERSION: 0.101.0
        HUGO_EXTENDED: true
        TARGET_REPO: DanielMwendwa/DanielMwendwa.github.io
        TOKEN: ${{ secrets.PORTFOLIO_TOKEN }}
        # CNAME: www.nidahcarlos.com
        HUGO_ARGS: --debug
        GO_VERSION: 1.17